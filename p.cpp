#include <iostream>
using namespace std;

void main()
{
	int traduzir, letras = 0, i, frase[100], j=0;
	char abc[5] = { 'a','e','i','o','u' };
   cout << " quantas letras tem a palavra";
   cin >> letras;
   cout << "qual é a frase \n";
   for (i = 0; i < letras; i++)
   {
	   cin >> frase[i];
   }
   for (i = 0; i < letras; i++)
   {
    if(frase[i]==abc[j])
   }
}